import styled from '@emotion/styled'
import { Flex } from '../../structural'

export const Container = styled(Flex)`
  .contact {
    margin-right: 20px;

    :last-child{
      margin-right: 0;
    }
  }
`